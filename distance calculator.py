#program to find distance from origin.
def distcalc():
    x= int(input('enter the x coordinate: '))
    y= int(input('enter the y coordinate: '))
    distance=(x+y)/2
    print('the distance from the origin is', distance)

distcalc()
